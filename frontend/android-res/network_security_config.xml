<?xml version="1.0" encoding="utf-8"?>
<!--
  android-res/network_security_config.xml
  Copie para: android/app/src/main/res/xml/network_security_config.xml

  Referência no AndroidManifest.xml:
  android:networkSecurityConfig="@xml/network_security_config"
-->
<network-security-config>
    <!-- Produção: somente HTTPS -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!--
        Desenvolvimento local: permite HTTP para o IP do servidor.
        Remova ou ajuste o IP antes de publicar na Play Store.
    -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">192.168.1.100</domain>
        <domain includeSubdomains="false">192.168.0.100</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
